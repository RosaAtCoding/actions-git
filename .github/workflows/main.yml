name: Test

on: [push]

jobs:
    test-build:
        runs-on: ubuntu-latest

        steps:
            - name: ls
              run: ls -al
            
            - name: install curl
              run: apt install curl